# Dev: local API.
# Production (no domain): use HTTPS API URL from a CloudFront distribution in front of your Elastic IP
# (not http://IP — HTTPS CloudFront sites block mixed content to http://).
# Vite bakes this in at build time — rebuild after changing.
VITE_API_URL=http://localhost:3000
